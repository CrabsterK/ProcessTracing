@using Microsoft.AspNet.Identity
@using Microsoft.Owin.Security
@using ProcessTracing.Models
@model ProcessTracing.Models.ExternalLoginListViewModel
@{

    var loginProviders = Context.GetOwinContext().Authentication.GetExternalAuthenticationTypes().FirstOrDefault();
    if (Request.IsAuthenticated)
    {
        using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
        {
            // akcja wylogowania a wyświetlają się dane użytkownika
        }
    }
    else
    {
        <button class="btn btn-outline-primary">@Html.ActionLink("Zaloguj się", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</button>     
        }

    }